{
    "version": 2,
    "rewrites": [{ "source": "/(.*)", "destination": "/chatting-api" }],
    "builds":[
        {
            "src": "./index.js",
            "use": "@vercel/node"
        }
    ]
}